import React from 'react';
import PropTypes from 'prop-types';

{{capitaliseName name}}.propTypes = {};

export default function {{capitaliseName name}}(props) {
    return (<React.fragment>{{capitaliseName name}}</React.fragment>);
}