import React from 'react';
import Modal from '..';

export default {
    title: '',
    component: Modal
};

export const Modal = () => {
	return(<React.Fragment>Modal</React.Fragment>)
};

Modal.story = {
    name: ''
};
